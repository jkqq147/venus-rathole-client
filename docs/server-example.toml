# This file belongs on your public server, not on Venus OS.

[server]
bind_addr = "0.0.0.0:2333"

# Create one service for each local target. Targets on the same GX share a token.
[server.services.gx-boat-01-ssh]
token = "A1B2C3D4"
bind_addr = "0.0.0.0:22201"

[server.services.gx-boat-01-web]
token = "A1B2C3D4"
bind_addr = "0.0.0.0:28080"

# Connect to server.example.com:22201 for SSH, or :28080 for the web target.
