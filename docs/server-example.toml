# This file belongs on your public server, not on Venus OS.

[server]
bind_addr = "0.0.0.0:2333"

# Create one service for each GX device. Keep every token unique.
[server.services.gx-boat-01]
token = "replace-with-a-long-random-token"
bind_addr = "0.0.0.0:22201"

# Connect to server.example.com:22201 to reach this GX device's SSH port.
